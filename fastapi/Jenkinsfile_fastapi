pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            steps {
                // Obtention du code depuis un référentiel Git
                checkout([$class: 'GitSCM', branches: [[name: '*/BAS_ALAIN']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'your-git-credentials', url: 'https://github.com/TeamMlops2023/Weather_Forecast.git']]])
            }
        }

        stage('Build and Test') {
            steps {
                // Insérez ici les étapes spécifiques de construction et de test de votre application.
                // Vous pouvez utiliser des commandes shell, exécuter des scripts, etc.
                // Assurez-vous d'ajouter vos propres commandes pour construire et tester votre application.
            }
        }
    }
}
