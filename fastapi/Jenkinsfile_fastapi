pipeline {
    environment { 
        DOCKER_ID = "mlops_weather"
        DOCKER_IMAGE = "teamlopsapi"
        DOCKER_TAG = "v.${BUILD_ID}.0"
    }
    agent any 
    stages {
        stage('Declarative: Checkout SCM') {
            steps {
                checkout scm
            }
        }
        stage('Docker Build') {
            steps {
                script {
                    sh '''
                    cd fastapi  # Assurez-vous que le répertoire fastapi existe dans votre projet
                    docker build -t $DOCKER_ID/$DOCKER_IMAGE:$DOCKER_TAG .
                    '''
                }
            }
        }
        stage('Docker Run') {
            steps {
                script {
                    sh '''
                    cd fastapi  # Assurez-vous que le répertoire fastapi existe dans votre projet
                    docker-compose up -d
                    sleep 10
                    '''
                }
            }
        }
        stage('Test Acceptance') {
            steps {
                script {
                    /*
                    Obtenir l'adresse IP du conteneur FastAPI
                    */
                    fastapi_ip=$(docker inspect -f '{{range .NetworkSettings.Networks.mon-reseau-docker.IPAddress}}' mlops_2023_fastapi_fastapi_1)
                    response=$(curl -s -o /dev/null -w "%{http_code}" $fastapi_ip:8000)
                    if [ "$response" = "200" ]; then
                        echo "Tout fonctionne parfaitement"
                    else
                        echo "Le test a échoué avec le code de réponse: $response"
                        exit 1
                    fi
                }
            }
        }
    }
}
