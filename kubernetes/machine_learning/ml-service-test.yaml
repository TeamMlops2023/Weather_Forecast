apiVersion: v1
kind: Service
metadata:
  name: ml-model-service
  namespace: test
spec:
  selector:
    app: ml-model
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      targetPort: 8000
