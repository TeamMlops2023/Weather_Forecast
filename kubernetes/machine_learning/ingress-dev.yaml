apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ml-ingress-dev
  namespace: dev
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /ml-dev
        pathType: Prefix
        backend:
          service:
            name: ml-model-service
            port:
              number: 80
