apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress-test
  namespace: test
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /fastapi-test
        pathType: Prefix
        backend:
          service:
            name: fastapi-service
            port:
              number: 8000
