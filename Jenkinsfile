pipeline {
    agent any

    stages {
        stage('Update') {
            steps {
                // Maj des modifs github
                checkout scm
                // A completer avec le reste des etapes
            }
        }
    }
}
